######
XMLRPC
######

This is test code to use Trigger's bundled XMLRPC server
(trigger.contrib.xmlrpc.server) and front-end it with SSL certification
verification.

The certs are found one directory up.
